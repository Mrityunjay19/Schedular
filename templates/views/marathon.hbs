<!DOCTYPE html>

<html>
    <head>
        <title>
            Marathon Scheduling
        </title>
        <link rel = "stylesheet" href="../css/sports.css"/>
    </head>

    <body>
        <img id = "image" src="../img/marathon.jpg"/>

        <h1 id = "heading">
            Marathon
        </h1>

        <section id="schedule"> 
            <div class="info" >Schedule</div>
            <ul>
                {{#each data}}
                    {{#if this.first}}
                        <h1>{{this.second}}</h3><br>
                    {{else}}
                        {{#if this.third}}
                            <h3>{{this.second}}</h2>
                        {{else}}
                            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;{{this.second}}<br>
                        {{/if}}
                    {{/if}}
                {{/each}}
            </ul>
        </section>

        <section >
            <div class= "info">Important Links :</div>
            <div><a class = "links" href="https://www.worldathletics.org/disciplines/road-running/marathon">MARATHON</a></div>
            <div><a class = "links" href="https://indianathletics.in/">AFI - Athletics Federation of India</a></div>  
        </section>

    </body>
</html>